<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.khan.designapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FoodOrderBottomSheet"></activity>
        <activity android:name=".BottomSheetsActivity" android:parentActivityName=".MainActivity" />
        <activity android:name=".BottomSheetGridActivity" />
        <activity android:name=".SimpleBottomSheetActivity" />
        <activity
            android:name=".BottomNavigationActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".NavigationWithFabActivity" />
        <activity android:name=".ShiftingNavigationActivity" />
        <activity android:name=".BottomAppBarClass" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>